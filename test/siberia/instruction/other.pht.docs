(category "other"
    (instr
        (long "progn")
        (desc """
            Блок инструкций.
            Может быть применён вне тела функции.
            """)
        (usage
            "(progn expr0 exprN)"
            [["expr*"] "инструкции."])
        (example
            """
            (use-ctx pht
                (progn
                    (test-fn 0 "Тестовый код №0")
                    (test-fn 1 "Тестовый код №1")
                    (test-fn 2 "Тестовый код №2")))
            """
            "Код работает на всех платформах!"))
    (instr
        (long "include")
        (desc """
            Подключение @i{одного} стороннего файла или @i{одной} директории с файлами.
            Подключаемый файл будет @b{парситься с нуля} - на него не будут воздействовать подлюченные или указанные в @i{uses} модули.
            @i{Выполняется во время парсинга.}
            """)
        (usage
            "(include file)"
            [["file"] "файл."])
        (usage
            "(include dir/*)"
            [["dir"] "директория."])
        (example [
            [
                """
                (include other.pht)
                """
                "Файл @bi{src.pht} модуля @b{test}."
            ]
            [
                """
                (use-ctx pht
                    (app-fn
                        (println "Привет из другого файла!")))
                """
                "Файл @bi{other.pht} модуля @b{test}."
            ]])))