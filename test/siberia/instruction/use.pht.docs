(category "use"
    (instr
        (long "load-ctx")
        (desc """
            Инструкция частичного использования модулей в локальном контексте блока.
            Производит добавление в список модулей - позволяет использовать:
            1. @i{парсеры}
            2. @i{депарсеры}
            3. @i{обработчики}
            4. @i{Компиляторы}
            Без загрузки модуля и обработки его исходных файлов.
            """)
        (usage
            "(load-ctx name0 nameN expr0 exprN)"
            [["name*"] "модули."]
            [["expr*"] "выражения блока."]))
    (instr
        (long "use-ctx")
        (desc "Инструкция использования модулей в локальном контексте блока.")
        (usage
            "(use-ctx name0 nameN expr0 exprN)"
            [["name*"] "модули."]
            [["expr*"] "выражения блока."])
        (example
            """
            (use-ctx pht
                (app-fn
                    (println "Внимание! Обнаружен безопасный код!")))
            """
            "Код работает на всех платформах!")
        (test "test/siberia/jvm/use-ctx")))